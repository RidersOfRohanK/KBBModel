package Adapter;

public interface fixAuto {
    public Object fixAuto(int errorno);
//    public void fixAuto(int errno, String str); // Going to need to make it flexible to take in different types of data
}
